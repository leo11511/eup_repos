<h1>Alle Events</h1>
<p><%= link_to "Neues Event erstellen", new_event_path %></p>
<p>Zeige folgende <% @events.size %> an:</p>
	<table>
		<tr>
			<th>Name</th>
			<th>Ort</th>
			<th>Preis</th>
			<th>Wann</th>
			<th>Beschreibung</th>
		</tr>
		<% @events.each do |event| %>
			<tr>
				<td><%= event.name %></td>
				<td><%= event.location %></td>
				<td><%= format_price(event) %></td>
				<td><%= event.start_at %></td>
				<td><%= truncate(event.descritpion, length: 25, seperator: ' ', omission: " ") %>
					<%= link_to "...", event_path(event.id) %>

				</td>
				<td><%= link_to "Show", event_path(event.id) %></td>
				<td><%= link_to "Edit", edit_event_path(event.id) %></td>
				<td><%= link_to "Delete", event_path(event.id), method: :delete, data: {confirm: "Sicher?"} %></td>
			</tr>
		<% end %>
	</table>

	<p>
		<%= link_to "Alle Events", showall_events_path, type: "button", class: "button sucess" %>
</p>